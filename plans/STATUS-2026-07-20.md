# PlanetProfile genai — current status & Machine B queue (2026-07-20)

Single current-state source of truth. Supersedes the day-by-day addenda
in HANDOFF-2026-07-09 (kept as the detailed log). Machine A = this M4
Air (authoring, GUI, smokes, artifact integration). Machine B = M2 Pro
(all heavy compute). Public app consumes origin/genai only.

## Deployed / integrated SBI slots (GUI `_SBI_ARTIFACT_SLOTS`)

| Slot | State | Notes |
|---|---|---|
| Titan (Andrade, no-ocean) Test50 8D | DEPLOYED | unchanged |
| Galileo–Europa v1.1 8D | **DEPLOYED 2026-07-20** | replaced v1 in place; honest-data framing; gates 8/8; GUI sampling verified |
| Clipper–Europa v4 geodesy 11D | **INTEGRATED, sampling verified — AWAITING USER RATIFICATION** | u-upper-limit deliverable verified; slot + u-panel live |
| Galileo–Europa v1 | RETIRED | replaced by v1.1 |
| Clipper–Europa v2 | RETIRED | replaced by v4 |
| Clipper–Europa v3 | VETOED | wrong k2 bounds; 2D cache reused by v4 |

## Since the last HF deploy (live = a882e599, 2026-07-18)

Everything below is on origin/genai, NOT yet on the public app (release
discipline: HF ships only when the user runs the one-liner):

- **main merged into genai** (523890a0): libration model
  (Gravity/Librations.py), DP-ALMA gravity backend, etaMelt plumbing,
  upstream bug-fix sweep. genai features preserved (MoonMag first-party,
  TidalPy backend, McCleskey speciation, Arrhenius viscosity, guarded
  reaktoro import). NO merges genai -> main (user directive). PP-pipeline
  BuildTest sweep still owed by Machine B.
- **v1.1 + v4 integrated** (6a463baa): slot swaps, retirements, the
  non-hydrostatic **u-panel** (reportable upper limit on
  u = dC22_nh + dC20_nh/3.324; per-component marginals marked do-not-cite).
- **Interactive globe** (globe v1->v3): textured NASA/USGS surface,
  always-cutaway with solid core, posterior sample picker, principal
  axes A/B/C, degree-2 figure from posterior k_f (hydrostatic fallback
  when a run has no C20/C22), exaggeration slider.
- **C/MR2 MacCullagh explainer**; per-sample connected-signals cloud plot.
- **Cassini–Enceladus test scaffold**: libration_deg observable channel
  (rigid 3-layer Van Hoolst), body-parameterized gravity keys
  (gravity_ref_radius_m / gravity_j2_over_c22), cache_builder
  bulk_overrides, smoke config + cache + smoke MCMC. Park et al. 2024
  (+ erratum) constraints; correlated (C20,C22) conditioning implemented.
- **Bug fixes**: Titan-k2-baseline leak (BODY_OBS_DEFAULTS + global-planet
  sync + config-load widget reseed); Im_h2 fold.
- **Perf**: Clairaut k_f 5x (interface-preserving downsample); induction
  Ae precompute persisted to a `<cache>.ae_sidecar.pkl` (v4 run
  6.0 -> 2.4 min; sidecars committed + shipped by the deploy script).

## Machine B queue (in order)

1. **BuildTest sweep** to close the main-merge PP-pipeline verification
   (Machine A verified the inference stack: 29/29 tests + both slots
   sampling-verified, but the full PP BuildTests need heavy compute).
   Report any regression from the libration/DP-ALMA/etaMelt merge.
2. **Cassini–Enceladus production** (no non-compute blockers remain; all
   three prior blockers resolved 2026-07-19: hydrostatic ratio 3.25 per
   McKinnon 2015; correlated 2x2 (C20,C22) conditioning implemented;
   elastic-libration correction shown unnecessary at 0.03%). Steps:
   - Dense Tb cache at Seawater 10 ppt with
     `bulk_overrides={'Cuncertainty': 0.08, 'CuncertaintyUpper': 0.08,
     'CuncertaintyLower': 0.08}` (the template's tight MoI window rejects
     off-default Tb nodes; the inference constrains MoI via observables).
     Box ~[271.8, 272.6] K at 10 ppt (272.7+ exceeds freezing); consider
     a salinity sensitivity leg.
   - Production config = smoke (configs/enceladus_cassini_smoke_6D.json)
     + dC20_nh U[-1.5e-3, 1.5e-3] + dC22_nh U[-1e-4, 1e-4] (asymmetric,
     mission-body-constraints.md); keep observable_correlations
     {"C20,C22": 0.47} and gravity_j2_over_c22 3.25. Reference MCMC ->
     Test/mcmc_results/Enceladus/ (add .gitignore negations). Full gate
     suite; the libration channel is the ocean/shell-decoupling
     constraint. Build the Ae sidecar after the cache lands (auto on
     first runner construct; commit it).
3. **Nothing else pending** — v1.1 + v4 delivered and integrated.

## Awaiting the USER

- **v4 ratification** -> Machine A flips the INDEX row to Deployed.
- **HF deploy** (`HF_SPACE_ID=... HF_TOKEN=... bash
  plans/scripts/build_deploy_branch.sh`) ships the whole batch above.
- Optional: default posterior draws 10k -> 5k to ~halve run time (no
  meaningful plot loss).

## Reference docs
- Constraints per mission-body: plans/mission-body-constraints.md
- 1D roadmap (six mission-body models, 3D/missionwidget future):
  plans/mission-body-1d-roadmap.md
- Campaign plans (v2/v3/v4): plans/europa-clipper-v*.md
- Gate reports: validation_reports/europa_{galileo_v1p1,clipper_v4}_1m/
- Artifact ledger: PlanetProfile/Inference/sbi_artifacts/INDEX.md
