# Europa v5/v6/v7 gate adjudication — 2026-08-02

Manager: Claude (Fable 5, Machine A). Independent scientific review: Opus 5
adversarial pass (full evidence audit; reimplemented the HEAD shape-excess
decomposition and validated it against committed reports). Vocabulary per the
2026-07-24 ruling: failures adjudicated acceptable are recorded
`FAIL-ADJUDICATED-ACCEPTABLE`, never relabeled PASS.

## Verdicts

| Campaign | Crosscheck | Limits | SBC | Ratification |
|---|---|---|---|---|
| v5 baseline | **FAIL survives at HEAD**: D_iceIh_km shape-EXCESS 0.0570 vs tol 0.0510 (1.12x, headline science param) + log10_zeta_Ih mean 1.074x | superseded-gate artifact; needs HEAD re-run on constructed grid | n=108, BELOW the documented >=200 minimum — calibration unestablished | **NOT RATIFIABLE** pending B1/B2 |
| v6 baseline | **clean PASS at HEAD** (0 shape-excess fails, 0 mean fails, sigma in band) — original FAIL was superseded-code artifact | same as v5 | n=102, below minimum | **CONDITIONALLY RATIFIABLE** after B1/B2/B6 |
| v7 open-\|Ae\| | FAIL is a **reference-side artifact** (see below); flow passes 22/22 mean+sigma gates vs the v5 reference, which is provably a valid reference for the identical true posterior | same | n=281, passes only after BH-FDR (criteria skew vs v5/v6 noted) | **BLOCKED** pending B3/B4/B5 |

GUI consequence: the `cb597490` not-ratified gating stays in force for ALL
v5/v6 slots (v7 was never wired). Nothing is ratified today.
**SUPERSEDED for v5/v6 by the Final ratification addendum (2026-08-06) at the
bottom of this file: v5 RATIFIED (scoped) + v6 RATIFIED; gating lifted.**

## Key evidence

1. **Limits.** Delivered reports were generated by pre-`3fa5a3fc` gate code
   (raw containment == 1.0 pooled over an Im_k2 0.05–0.30 sweep with
   reject_outside_prior=False — unreachable for NSF flows; deploy path
   rejects out-of-box by construction). A Machine A re-run at HEAD passes all
   three (in-support containment 0.982–0.995, materiality shift
   0.006–0.013 sigma vs 0.25 bar) BUT reused the legacy sweep, leaving only
   one in-support point — HEAD's own `_build_default_sweep_grid` would have
   refused (<3 in-support points). The overturn is therefore provisional:
   official regeneration must use HEAD defaults (B1) and anchor mode (B6).
   The v5 report also failed monotonicity (one +0.166 dex violation at the
   0.05→0.10 step); resolved by the ratified 2026-07-09 falsified-window
   rule (monotone premise invalid below Im_k2 ~0.15), stated here
   explicitly. Post-hoc caveat acknowledged: the containment excuse was used
   in v1/v2 adjudications before it entered gate code; what rescues v5–v7 is
   the NEW in-support restriction, discharged only by B1/B6.

2. **Crosscheck code vintages differ per campaign.** v5 (2026-07-21) and v6
   (2026-07-23) reports predate the `916d2d07` shape-excess clause
   (2026-07-24); v7 has it. Recomputed at HEAD: v4 0/11 shape-excess fails,
   v6 0/11, v7 0/11, v5 1/11 (D_iceIh_km). Reports do not record the
   gate-run code SHA — provenance was inferred from field presence (B1 adds
   the SHA).

3. **v7 reference inconsistency (proof).** v7 config = v5 config with
   induction support strictly enlarged (amp_min cuts removed, phase caps
   30→70 deg); same cache + sidecar (SHA256-identical per manifests); x_obs
   byte-identical. Sidecar lookup over both reference posteriors: mass in
   the newly admitted region (|Ae|<0.7 or phase>30 deg) = 0.000000 in BOTH
   (posterior sits at |Ae|~0.86, phase ~6.6 deg, q99 8.4 deg). Hence the two
   true posteriors are identical — yet the references differ by 1.06 km in
   D_iceIh mean (~14x quoted MC error), the shift has the WRONG SIGN for a
   support effect (new region is thick-ice → up; observed down), and it is a
   near-rigid translation (sigma ratio 0.975, all quantiles shifted ~1 km) —
   the signature of nested-sampling correlated log-volume error, not of
   added support. Which reference wandered is OPEN: v5's log_Z_err (0.281 vs
   v7's 0.117) and its sample count (4,343 vs ~19,750 implied by its own
   error estimate at n_live=500) make v5 the more suspect run. ΔlogZ
   consistency (must equal −ln(V7/V5) ≈ −0.92; observed −0.25±0.30, ~2.2σ
   tension) is B4.

4. **Persistent D–w degeneracy-direction signal — carried as the top open
   scientific risk.** Every campaign with statistical power flags the same
   degenerate pair (corr ~0.92): v4 SBC FAIL on Tb_K + log10_wOcean_ppt at
   n=1000; v5 HEAD shape-excess on D_iceIh_km; v7 crosscheck means on
   D_iceIh_km + log10_wOcean_ppt. v5/v6 SBC "passes" at n~105 have no power
   to see a v4-sized defect (its Tb defect would show p~0.8 at n=108). B2
   and B5(iii,iv) test this directly.

5. **v4 ledger correction (governance).** The deployed v4's own reports show
   crosscheck mean breaches on log10_zeta_Ih (1.23x tol) and rho_core_kgm3
   (1.18x, median also failed) and SBC verdict FAIL (Tb_K p=0.000407,
   log10_wOcean_ppt p=0.0338, n=1000), none disclosed in PHASE_GATES.md
   ("not location or scale errors") or the INDEX row. v4 deployment stands
   (user-ratified) but the record must disclose; whether v4 needs
   re-adjudication is a USER decision. INDEX row corrected this session.

## Scoped Machine B follow-ups (compute)

B1 Regenerate ALL gate reports at HEAD (baselines + arms): HEAD default
   sweep construction (no --sweep-values), gate-code SHA recorded, BH-FDR
   applied uniformly; reconcile v{5,6,7}_gate_summary.json.
B2 SBC re-runs at n_sbc_pairs >= 500 for v5/v6 baselines (spec minimum is
   200; v4 ran 1000). Explicitly report D_iceIh_km / log10_wOcean_ppt / Tb.
B3 Reference wander: re-run BOTH v5 and v7 references, >=3 seeds each, same
   pinned environment (scipy straddle 1.16.3/1.17.1 noted), n_live >= 2000.
   Success criterion: between-seed scatter brackets the observed 1.06 km.
   Do NOT assume v7 is the outlier.
B4 Evidence-ratio check: ΔlogZ vs −ln(V7/V5) with V7/V5 computed by Monte
   Carlo over the prior under the two induction-bound sets.
B5 Preregistered v7 flow-side diagnostics (before any ratification path):
   (i) flow-vs-flow v5/v7 at the fiducial (reference-free; currently
   0.64 km / 0.006 dex); (ii) v7-flow-vs-v5-reference formal crosscheck with
   shape clause; (iii) synthetic-truth bias test; (iv) local
   expected-coverage (TARP) near the fiducial — SBC over the prior cannot
   see conditioning-point-specific defects.
B6 Limits anchor mode (W1 <= 0.25 sigma vs ground-truth MCMC) at reachable
   Im_k2 values, all three campaigns.
B7 Documentation: record outcomes with FAIL-ADJUDICATED-ACCEPTABLE
   vocabulary; never relabel PASS.

Machine A side of B7 done this session: v4 INDEX row corrected; STATUS.md
updated; this record committed. Titan freegrav gates (containment 0.975
old-gate; dC20/dC22 mean fails) get a separate pass after B1 lands.

## Addendum — B3 outcome (2026-08-05)

B3 multi-seed references (n_eff=2000, seeds 101/202/303, pinned env) resolve
the open reference question and correct one claim above:

- The 1.06 km v5-v7 reference disagreement was a resolution artifact of the
  n_eff=500 runs. At matched resolution the gap is −0.19 ± 0.22 km (inside
  the 0.18 km between-seed floor) — consistent with the proof that the two
  posteriors are identical. NEITHER old reference was singly at fault; both
  wandered (fresh means: v5 61.35–61.65 km, v7 61.61–61.80 km, vs old
  62.1 / 61.0).
- CORRECTION: "v7 flow passes 22/22 mean+sigma gates against the v5
  reference" was measured against the old wandered v5 reference and is
  void. Against the fresh references: v5-flow D_iceIh +0.6–0.8 km (inside
  the 0.25σ tolerance), v7-flow +1.1–1.3 km (at/near tolerance). Both
  flows offset in the SAME direction — the D–w degeneracy-direction risk
  flagged above stands and is now the sharpest open question for B2/B5.
- All §0.7 crosscheck re-evaluations target the pooled fresh references;
  the preregistered v5 shape-excess floor is 0.36 km (2× the empirical
  0.18 km between-seed floor, per reviewer).

## Final ratification — 2026-08-06 (manager, after B1/B2/B6 vs fresh pooled reference)

Inputs: §0.11 of MACHINE-B-HANDOFF; `validation_reports/v5_gate_summary.json`
(carries `reviewer_adjudication_2026_08_06`), `v6_gate_summary.json`, per-arm
dirs. Machine A spot-verified the headline numbers against
`europa_clipper_v5_baseline_1m/sbc/sbc_report.json` (dC22_nh raw p=0.0016779,
BH-adj 0.0184566, c2st 0.5547; 621 kept pairs) — matches B's report.

**v5: RATIFIED (scoped).** SBC at n=1500: 10/11 PASS;
`dC22_nh = FAIL-ADJUDICATED-ACCEPTABLE` — a real, mild, v5-flow-specific
undertraining of the most sharply compressed nuisance (prior/σ ~100:1;
monotone rank-CDF deviation; v6's identical nuisance passes p=0.61, so not a
free-gravity-family defect; the B2 n→1500 fix exposed it, old n=108 masked it).
The deployed deliverable (D_iceIh / ocean state / salinity) is doubly
validated: SBC-clean marginals (D_iceIh p=0.996, log10_w p=0.279) AND
crosscheck PASS vs the fresh pooled n_eff≈2000 reference with D_iceIh
d_excess 0.0171 ≤ 0.0281 even WITHOUT the preregistered 0.36 km floor.
Mandatory scope note in the GUI: dC22_nh marginal and any
non-hydrostaticity readout are NOT v5 deliverables — deferred to v6, which
owns them cleanly. A 2nd-seed SBC persistence check is queued NON-BLOCKING
(confirmation, not a ship gate; no retrain warranted for the deployed
deliverable).

**v6: RATIFIED.** All gates PASS at HEAD: SBC 11/11 at n=1500 (dC22_nh
BH-adj p=0.85), crosscheck PASS (stock, no floor), limits PASS.

**B6 (limits anchor): N/A-by-domain for v5+v6** — the reachable Europa Im_k2
window sits entirely below the 0.15 MCMC-falsified boundary, so the anchor
set is empty; the single free anchor (Im_k2=0.004) already passes W1 on the
pooled reference. Recorded as domain-limited N/A, not a skipped gate. Tb_K is
derived (PCHIP inversion of D_iceIh), not sampled — no SBC row; summaries
carry the explicit `derived_params_sbc_na.Tb_K` statement.

**GUI consequence:** `cb597490` not-ratified gating LIFTED for the six v5/v6
slots. v5 slots carry the scope note above; v6 slots ship clean. v7 remains
BLOCKED pending B4/B5 (unchanged).
