# Active operational scripts

- `plans/scripts/build_deploy_branch.sh` — the only supported public-app
  snapshot/deploy path. Keep its stable path because README and deployment
  documentation invoke it directly.
