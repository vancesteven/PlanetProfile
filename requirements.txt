# PlanetProfileApp serving requirements (Streamlit Community Cloud / pip).
# Pins match the cross-version-validated environment (see
# PlanetProfile/Inference/sbi_artifacts/INDEX.md): torch 2.8.x is the
# validated runtime for the deployed 2.11-trained SBI artifacts.
# The CPU wheel index keeps torch ~200 MB instead of the ~2.5 GB CUDA build.
# NOTE: reaktoro (CustomSolution chemistry) is conda-forge-only and
# intentionally absent; the app degrades gracefully without it.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.59.1
numpy==2.4.3
scipy==1.17.1
matplotlib==3.10.9
pandas==3.0.2
plotly==6.6.0
corner==2.3.0
cmasher==1.9.2
seaborn

torch==2.8.0
sbi==0.26.1

TidalPy==0.7.4
CyRK==0.17.1
pocomc==1.2.6

SeaFreeze==1.0.0
gsw==3.6.21
spiceypy==8.1.0
obspy==1.5.0
hdf5storage==0.2.0
h5py==3.14.0
# mpmath unpinned: conda ships 1.4.x but PyPI tops at 1.3.x and torch's
# sympy dependency pins below 1.4 — let pip resolve.
mpmath
openpyxl
PyYAML==6.0.3
psutil==7.2.2
sqlalchemy==2.0.49
pdf2image
PyALMA3==1.0.1
